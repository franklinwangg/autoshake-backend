{
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "esbuild content.ts popup.ts background.ts --bundle --outdir=dist --platform=browser",
    "watch": "esbuild content.ts popup.ts background.ts --bundle --outdir=dist --platform=browser --watch"
  }
}
